/// A placeholder class that represents an entity or model.
class CompanyReferential {
  const CompanyReferential(this.name, this.id, this.iconPath);

  final String name;
  final int id;
  final String iconPath;

}